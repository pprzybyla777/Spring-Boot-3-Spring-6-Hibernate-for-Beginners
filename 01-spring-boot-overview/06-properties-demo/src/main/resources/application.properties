# Use "*" to expose all endpoints or use comma-delimited list to expose individual endpoints
# management.endpoints.web.exposure.include=health,info
management.endpoints.web.exposure.include=*
management.info.env.enabled=true

# management.endpoints.web.exposure.exclude=*

info.app.name=My Super Cool App
info.app.description=A crazy fun app!
info.app.version=1.0.0

#
# Define my custom properties
#

coach.name=Mickey Mouse
team.name=The Mouse Club

#
# Change Spring Boot embedded server ports
#

server.port=7070

#
# Set the context path of the application
#
# All request should be prefixed with /mycoolapp
#
# server.servlet.context-path=/mycoolapp
